#env example file for the airflow host
#copy to deploy/airflow/.env (gitignored) and fill in real values
METEOSERVER_API_KEY=
SUPABASE_URL=
SUPABASE_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

VAPID_SUBJECT=mailto:demo@example.com
#if u want to use HTTPS instead of ssh otherwise leave blank
GH_PAT=
